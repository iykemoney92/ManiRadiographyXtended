import React from 'react';
import {IonFooter, IonTitle, IonToolbar} from '@ionic/react';

const Footer: React.FC<any> = () => {
  return (
    <IonFooter>
    <IonToolbar>
      <IonTitle>Copyright I.K Mani Services</IonTitle>
    </IonToolbar>
  </IonFooter>
  );
};

export default Footer;
